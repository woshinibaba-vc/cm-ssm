<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kk.mapper.BookMapper">
    <insert id="addBook" parameterType="com.kk.pojo.Books">
        insert into books
        values (#{bookName},#{bookCounts},#{detail})
    </insert>

    <delete id="deleteBook" parameterType="int">
        delete from books
        where bookID=#{bookID}
    </delete>

    <update id="updateBook" parameterType="com.kk.pojo.Books">
        update books
        set bookName=#{bookName},bookCounts=#{bookCounts},detail =#{detail}
        where bookID = #{bookID}
    </update>

    <select id="getBook" parameterType="int" resultType="com.kk.pojo.Books">
        select * from books
        where bookID=#{bookID}
    </select>

    <select id="getBookList" resultType="com.kk.pojo.Books">
        select * from books
    </select>

</mapper>